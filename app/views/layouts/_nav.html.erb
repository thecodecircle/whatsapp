<%= link_to "עוד אחד", root_path %>
<% if cookies[:chats].present? %>
	<% JSON.parse(cookies[:chats]).each do |id| %>
		<% chat = Chat.find(id) %>
		<%= link_to chat.title, chat %>
	<% end %>
<% end %>
